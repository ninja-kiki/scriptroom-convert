# 변경 이력 (scriptroom convert)

## v0.6.0 — 자막을 교체 대신 말투 근거로
- **말투 가이드 보기·편집·재번역** — 자막 근거로 만든 인물별 말투 가이드를 작업 화면에서 확인·수정. 의역/호칭이 어긋나면 고치고 "이 가이드로 다시 번역"(UI 버튼, 토큰 재사용).
- **리더 검토 필터** — 번역 뷰에서 공식 자막과 표현이 다른 줄을 노랑 점선으로 표시, "검토 N" 버튼으로 그 씬만 ◀▶로 넘기며 스팟체크.
- **온보딩 정비** — Claude 미설치/미로그인 시 상단 배너·오류분류로 안내(서버는 안 죽음). README를 멀티세션·자막 재설계에 맞게 갱신.
- **헤딩 씬번호 글루 제거** — `DAY1`·`MORNING2`·`NIGHT14` → `DAY`·`MORNING`·`NIGHT` (장소번호 `ROOM 237`은 보존).
- **자막 텍스트 교체 전면 제거** — 기존엔 번역 대사를 공식 자막 줄로 덮어썼는데, 짧은 줄 우연 일치로 엉뚱한 자막에 덮이는 치명적 오역이 있었음(greenberg 169줄·spider 12줄). 이제 자막은 번역을 **절대 안 바꿈**.
- **자막 기반 말투 글로서리** — 작품당 1회, 영어 대사 + 공식 한국어 자막을 함께 보고 인물별 말투(반말/존댓말)·호칭·의역 톤을 **공식 자막을 근거로** 추출 → 모든 씬 번역에 주입. 자막의 판단이 번역에 녹아들되 글자는 한 목소리로 일관.
- **씬별 자막 주입 제거** — 매 씬 프롬프트에 자막 통째 주입하던 것 제거 → 토큰 급감(@DAN 4,880→수백대), 앵커링 위험 제거.
- **자막 정렬은 참고/검토용 메타로만** — 번역↔공식자막 확신 정렬을 계산해 리더에서 점선 표시(표현 다르면 노랑=검토 신호). 교체 아님.
- **자막 매칭 임계값 길이 기반화** — 짧은 줄일수록 거의 정확해야 정렬로 인정(오매치 차단).

## v0.5.0 — 멀티세션 + 백그라운드 무중단
- **작업 루프를 서버로 이동** — 포맷→말투→번역 루프를 브라우저가 아닌 서버 프로세스가 소유. 이제 **탭을 닫거나 최소화해도 작업이 계속**됨(브라우저 throttling 무관).
- **멀티세션** — 한 탭에서 여러 작품을 동시에 백그라운드로 변환. 홈 '작업' 목록에 각 작품의 진행이 실시간 카드로 표시(진행 중·일시정지·완료). 더 이상 탭 여러 개 띄울 필요 없음.
- **덮어쓰기 사고 차단** — 진행 상태를 단일 localStorage 슬롯이 아니라 **서버가 잡별 파일로 영속**. (이전엔 탭 두 개가 같은 슬롯을 공유해 한 작품이 다른 작품에 덮여 사라지는 문제가 있었음.)
- **재시작 무중단** — 잡 상태를 씬 단위로 디스크에 저장. 서버 크래시·재시작·맥북 sleep 후 깨어나면 **마지막 완료 씬부터 자동 이어서** 처리.
- **전역 동시성 상한** — 여러 잡이 동시에 돌아도 claude 호출 총량을 한 상한으로 묶어 폭주 방지. per-job 동시성 설정 존중.

## v0.4.0 — 말투 일관성 + 자막 매칭 개선
- **인물 말투 사전(자동)** — 번역 전 작품당 1회, 대사 샘플로 인물별 말투(반말/존댓말)·호칭 가이드를 만들어 *모든 씬 번역에 주입*. 씬이 갈려도 한 인물의 말투가 일관되게 유지됨 (예: 코브→사이토 존댓말 고정). 자막 없는 작품에도 적용.
- **자막 매칭 알고리즘 개선** — 글자 자카드 → 오버랩계수+토큰정확×길이호환성. 같은 뜻 다른 표현도 매칭 (실측 her 10→53%, Leon 20→57%), 오탐은 안 늘림.
- **검수 추가 탐지** — 시간/전환 슬러그가 @화자로 잘못 태깅된 것(의심 화자) 플래그.

## v0.3.0 — 검수 도구 + 추출 정밀화
- **번역본 검수 기능 추가** (수정 탭) — 완료된 _translated.txt 여러 개를 넣어 메타·러닝헤더·중복·EN+KO중복·줄나눔·미번역 의심을 코드로 검사하고 안전한 결함은 자동수정 다운로드. **토큰 0.**
- **추출 정밀화** — 페이지마다 반복되는 러닝헤더/푸터(작품명+페이지번호, 드래프트 표시 등) 자동 제거. 들여쓰기·OCR 각본의 씬 감지 개선.
- **출력 정리** — LLM 메타 코멘트("타이틀 페이지 제거됨", 빈 씬 거부 응답 등) 제거, 줄나눔 일관화(@·# 앞 빈 줄).
- **토큰 절감** — claude 호출에서 기본 시스템프롬프트+툴 정의 제거(호출당 ~13k↓), 모델 분리.
- **디자인** — scriptroom 바우하우스 결로 통일, 라이트/다크 테마, 상태색(완료=파랑·진행=노랑·문제=빨강).
- **안정화** — 새로고침 시 홈 복귀, 작업기록 완료수 정확화, 씬 펼침 크래시 수정.

## v0.1 (최초 공유)
- PDF/자막 → 포맷 + 한국어 번역 (Max `claude -p`), 리더 연동.
